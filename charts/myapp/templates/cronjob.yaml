apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Values.app.name }}-cronjob
  namespace: {{ .Values.namespace }}
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: {{ .Values.app.name }}
        spec:
          restartPolicy: OnFailure
          containers:
            - name: {{ .Values.app.name }}
              image: hashicorp/http-echo:0.3.1
              args:
                - "-text=Hello from CronJob"