apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ .Values.app.name }}-daemonset
  namespace: {{ .Values.namespace }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.app.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.app.name }}
    spec:
      containers:
        - name: {{ .Values.app.name }}
          image: hashicorp/http-echo:0.3.1
          args:
            - "-text=Hello from DaemonSet"
          ports:
            - containerPort: 5678